<UserControl x:Class="DesktopClient.Views.AdminCreateUserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopClient.Views"
        mc:Ignorable="d">
    <StackPanel Orientation="Vertical" Margin="10,0">
        <Label Content="Create user" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <Label Content="Username"/>
        <TextBox Style="{StaticResource AdminTextBox}" Text="{Binding TmpUserName}"/>
        <Label Content="Password"/>
        <TextBox Style="{StaticResource AdminTextBox}" Text="{Binding TmpPassword}"/>
        <Label Content="Email"/>
        <TextBox Style="{StaticResource AdminTextBox}" Text="{Binding TmpEmail}"/>
        <Label Content="Is Administrator"/>
        <CheckBox Style="{StaticResource AdminTextBox}" IsChecked="{Binding TmpIsAdmin}"/>
        <Button Content="Create user" Height="25" Command="{Binding CreateUserCommand}"/>
    </StackPanel>
</UserControl>
