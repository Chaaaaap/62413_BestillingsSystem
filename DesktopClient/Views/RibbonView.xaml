<UserControl x:Class="DesktopClient.Views.RibbonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cc="clr-namespace:WPF.Controls;assembly=WPF"
             mc:Ignorable="d" 
             d:DesignHeight="56" d:DesignWidth="800">
    <Grid Background="{StaticResource RibbonBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
        </Grid.RowDefinitions>

        <Button>
            <Button.Content>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" />
                    <TextBlock Grid.Column="1" Text="Fed knap til noget"/>
                </Grid>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
